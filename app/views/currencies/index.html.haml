%h1 Listing currencies

%table.table.table-striped.table-condensed.table-hover
  %thead
    %tr
      %th Shortname
      %th Longname
      %th
      %th
      %th

  %tbody
    - @currencies.each do |currency|
      %tr
        %td= currency.shortname
        %td= currency.longname
        %td= link_to t('Show'), currency
        %td= link_to t('Edit'), edit_currency_path(currency)
        %td= link_to t('Destroy'), currency, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to t('New Currency'), new_currency_path
